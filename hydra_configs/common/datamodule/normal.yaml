# @package _global_
defaults:
  - default.yaml

datamodule:
  basic:
    input_size: 224

  data_module:
    _target_: dlhelper.dataset.PLDataModule
    dataset_name: ${datamodule.dataset}
    base_dir: ${datamodule.basic.base_dir}
